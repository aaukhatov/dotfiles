#!/usr/bin/env bash

git config --global user.name "Artur Aukhatov"
git config --global user.email "aaukhatov@gmail.com"
git config --global color.ui true
git config --global color.diff auto
git config --global core.editor vim
